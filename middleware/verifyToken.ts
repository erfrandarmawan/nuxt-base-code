// Import store and components
import { userStore } from '../stores/user'

export default defineNuxtRouteMiddleware((to, from) => {
  // // Define store variable
  // const user = userStore();
  // if (!user.userData.accessToken && to.path !== '/'){
  //   return navigateTo('/');
  // }
})